services:
  - type: web
    name: weather-prediction-app
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
    healthCheckPath: /health